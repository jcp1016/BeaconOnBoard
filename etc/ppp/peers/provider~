## example configuration for a dialup connection authenticated with PAP or CHAP
#
# This is the default configuration used by pon(1) and poff(1).
# See the manual page pppd(8) for information on all the options.

# MUST CHANGE: replace myusername@realm with the PPP login name given to
# your by your provider.
# There should be a matching entry with the password in /etc/ppp/pap-secrets
# and/or /etc/ppp/chap-secrets.
# comment this to get daemon mode pppd

# Serial device to which the modem is connected. (/dev/ppp -> /dev/MFD1)
/dev/ttyMFD1

# Speed of the serial line
19200

nodetach
#debug
#record ppplog.txt

# Detach from terminal once ppp connection is established
#updetach

# Use the DNS servers from the phone
#usepeerdns

# Use hardware flow control
#crtscts

# pppd must not propose any IP address to the peer
noipdefault

# Add the ppp interface as default route to the IP routing table
defaultroute

persist

passive

# Ignore carrier detect signal from the modem
#local

noccp
nopcomp
novjccomp
nobsdcomp
noaccomp
nodeflate

# Try with compression
# deflate 15,15

# Accept peers idea of our local address
ipcp-accept-local
ipcp-accept-remote

noauth
silent
#nomagic

connect /etc/ppp/peers/tmobile.chat
disconnect /etc/ppp/peers/disconnect.chat
