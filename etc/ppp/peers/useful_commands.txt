# View the active comm ports 
ip a

# Start the GPRS modem
pppd call provider

# Read the pppd and chat logs
journalctl -xf | grep -e "\(pppd\|chat\)"

# Test the internet connection
Turn off wifi on the computer
Connect wire from serial port (bottom) to computer (rhs)
screen /dev/cu.usbser... 115200 -L
curl a website to verify connectivity and data transfer 

# To Do
# Verify that we can send data to AWS
# Send SMS text to my phone

